# Patch 01 — 数值压缩 + 文案修复 + 行动按钮反馈

## 文件位置
`C:\Users\69032\Downloads\demo-pet\demo-producer\delay-game.jsx`

---

## 改动清单

### 1. 基础进度压缩
`BASE_PROGRESS` 从 `6` 改为 `3`。

同步检查并调整以下事件的 `progress` effects，确保压缩后数值比例合理：
- 整体原则：正面效果事件的进度 bonus 相对压缩（但可以小幅保留，让行动仍有意义）
- 负面效果事件的进度 penalty 也同比压缩，不然惩罚会太狠

---

### 2. 行动按钮加数值标签

`ActionMenu` 组件里的每个行动按钮，在 label 旁边加上效果预览标签。

格式：按钮内在 label 下方（或右侧）用小字显示效果，例如：

```
🔨 冲进度   [进度 +5  士气 -4]
💬 团队关怀  [士气 +10]
🆘 危机安抚  [士气 +25]
```

样式要求：
- 小字（fontSize: 10），monospace
- 正数绿色 `#4ade80`，负数红色 `#f87171`
- 数字直接显示（行动是机械操作，透明是对的）

---

### 3. 事件选项箭头预览保留方向，去掉具体数字

`ChoicePreview` 组件目前已经用箭头（`effectArrows`）显示方向，不显示数字——确认这个逻辑没有被改动，保持原样即可。

---

### 4. 文案修复

**zombie_reveal 对话框**（`ZOMBIE_REVEAL.dialogue`）：

原文：
> 所谓「进度40%」，其实包括了：外包的三套废弃美术资产、从没跑通过的技术demo，以及PM写的那份永远不会实现的设计文档。

改为：
> 所谓「进度40%」，其实包括了：外包的三套废弃美术资产、从没跑通过的技术demo，以及某个策划写的那份永远不会实现的设计文档——对了，那个策划已经离职了，没人记得他叫什么。

---

**impulse（灵机一动）事件对话框**（`impulse.dialogue`）：

原文：
> 我最近在玩《原神》，好可爱！我们也做一个向导角色！还有《黑神话》的变身机制！都加上吧！

改为（使用虚构游戏名）：
> 我最近在玩《星境传说》，好可爱！我们也做一个向导角色！还有《封神演义：觉醒》的变身机制！都加上吧！

---

## 不改的东西

- 事件选项（A/B/C）的 `ChoicePreview` 箭头逻辑不动
- 所有状态逻辑、游戏流程不动
- 样式风格保持一致（dark theme，monospace）
